# Homework 2e Report

I first started with the code provided in class, specifically the codebot and the tools files and the docker files to create a docker file. 

I first had a hard time figuring out the docker containers but at the end I got the hand of it. 

I then explored how to use the web_serch tool and it was really interesting using it. I mainly used it for things such as serching for the weather, but it was really nice seeing the ability to get real-time information that otherwise I would not be able to do. 

Then I experimented with the execute_code tool. It was really cool. I do encountered times that it did not work as I expected, for example I asked for a number in the fib series and instead of executing code, it didn't. Same with asking of the cosine of 63 degrees. I had to explicitely ask the model to execute the code.

However I did made it create a really cool tic-tac-toe game. 

Some limitations that I did noticed is that when giving it a link, it had a hard time accessing it. I tried giving it the instructions of the CS 236 project but it could not access the instructions. Additionally, it also had a hard time creating files, as I was unable to make it create files in my computer, which makes sence since the code is executed in a container instead of my machine.

I was really nice seeing and using these abilities and I am looking foward to use them in the future. 